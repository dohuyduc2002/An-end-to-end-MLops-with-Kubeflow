apiVersion: v1
kind: Service
metadata:
  name: evidently-ui
spec:
  type: {{ .Values.service.type }}
  selector:
    app: evidently-ui
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}

# PIPELINE DEFINITION
# Name: gold-layer-etl
# Inputs:
#    application_silver_status: system.Dataset
#    merged_silver_status: system.Dataset
#    minio_access_key: str
#    minio_bucket: str
#    minio_endpoint: str
#    minio_secret_key: str
#    namespace: str
#    yaml_object: str
# Outputs:
#    final_status: system.Dataset
#    submitted_spec: system.Artifact
components:
  comp-gold-layer-etl:
    executorLabel: exec-gold-layer-etl
    inputDefinitions:
      artifacts:
        application_silver_status:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
        merged_silver_status:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
      parameters:
        minio_access_key:
          parameterType: STRING
        minio_bucket:
          parameterType: STRING
        minio_endpoint:
          parameterType: STRING
        minio_secret_key:
          parameterType: STRING
        namespace:
          parameterType: STRING
        yaml_object:
          parameterType: STRING
    outputDefinitions:
      artifacts:
        final_status:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
        submitted_spec:
          artifactType:
            schemaTitle: system.Artifact
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-gold-layer-etl:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - gold_layer_etl
        command:
        - python3
        - -m
        - kfp.dsl.executor_main
        image: docker.io/microwave1005/kfp-etl:0.0.1
pipelineInfo:
  name: gold-layer-etl
root:
  dag:
    outputs:
      artifacts:
        final_status:
          artifactSelectors:
          - outputArtifactKey: final_status
            producerSubtask: gold-layer-etl
        submitted_spec:
          artifactSelectors:
          - outputArtifactKey: submitted_spec
            producerSubtask: gold-layer-etl
    tasks:
      gold-layer-etl:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-gold-layer-etl
        inputs:
          artifacts:
            application_silver_status:
              componentInputArtifact: application_silver_status
            merged_silver_status:
              componentInputArtifact: merged_silver_status
          parameters:
            minio_access_key:
              componentInputParameter: minio_access_key
            minio_bucket:
              componentInputParameter: minio_bucket
            minio_endpoint:
              componentInputParameter: minio_endpoint
            minio_secret_key:
              componentInputParameter: minio_secret_key
            namespace:
              componentInputParameter: namespace
            yaml_object:
              componentInputParameter: yaml_object
        taskInfo:
          name: gold-layer-etl
  inputDefinitions:
    artifacts:
      application_silver_status:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
      merged_silver_status:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
    parameters:
      minio_access_key:
        parameterType: STRING
      minio_bucket:
        parameterType: STRING
      minio_endpoint:
        parameterType: STRING
      minio_secret_key:
        parameterType: STRING
      namespace:
        parameterType: STRING
      yaml_object:
        parameterType: STRING
  outputDefinitions:
    artifacts:
      final_status:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
      submitted_spec:
        artifactType:
          schemaTitle: system.Artifact
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.12.1
